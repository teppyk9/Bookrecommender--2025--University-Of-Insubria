CREATE TABLE SESSIONI_LOGIN (
    ID SERIAL PRIMARY KEY,
    IDUTENTE INT REFERENCES UTENTI(ID) ON DELETE CASCADE,
    IP_CLIENT TEXT NOT NULL,
    TOKEN TEXT UNIQUE NOT NULL,
    LOGIN_TIME TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);